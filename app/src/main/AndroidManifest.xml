<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".nakonRegistracije"></activity>
        <activity android:name=".ImasPsa" />
        <activity android:name=".Objava" />
        <activity android:name=".glavni_screen" />
        <activity android:name=".activity_vlasnikRegistracija" />
        <activity android:name=".activity_ponude" />
        <activity android:name=".register" />
        <activity
            android:name=".login"
            android:label="Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>